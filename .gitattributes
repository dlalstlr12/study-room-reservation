# 기본: 텍스트 자동 판별, 저장소에는 LF로 정규화
* text=auto eol=lf

# Windows 전용 스크립트는 CRLF 유지
*.bat text eol=crlf
*.cmd text eol=crlf

# Gradle Wrapper 스크립트는 LF 고정 (CRLF면 실행 깨짐)
gradlew text eol=lf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
*.ico binary
